# SWEG-SSMP

## Setup

### Create Virtual Environment

Windows:
```batch
python -m venv venv
venv\Scripts\activate
```

macOS/Linux:
```bash
python3 -m venv venv
source venv/bin/activate
```

### Install Package
```bash
pip install -e .
```